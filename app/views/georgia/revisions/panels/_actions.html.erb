<% if policy(@revision).draft? and !%w(draft review).include? @revision.status and !@draft_revision %>
  <%= link_to 'Start a new draft', [:draft, @page, @revision], class: 'btn btn-primary' %>
  <hr />
<% end -%>