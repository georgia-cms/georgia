<% if @picture.new_record? %>
$("#new_picture").prepend("<div class='alert alert-error'><button type='button' class='close' data-dismiss='alert'>×</button><%= j @picture.errors.full_messages.join(', ').html_safe %></div>");
<% else %>
  $("#assets").prepend("<%= j render(@picture) %>");
<% end %>