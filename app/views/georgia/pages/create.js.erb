<% if @page.valid? %>
window.location.href = '<%= georgia.edit_page_path(@page) %>';
<% else -%>
console.log('<%= @page.errors.full_messages %>');
$('#new_page .modal-body').prepend("<div class='alert alert-error'><button type='button' class='close' data-dismiss='alert'>×</button>You need a valid and unique page title. Your page title can only consist of letters, numbers, dash (-) and underscore (_)</div>");
$('input#page_title').closest('.control-group').addClass('error');
<% end %>